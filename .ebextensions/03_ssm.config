# SSM Agent configuration for Session Manager access
# Allows secure shell access without SSH keys

commands:
  01_start_ssm:
    command: "systemctl enable amazon-ssm-agent && systemctl start amazon-ssm-agent"
